{% extends 'base.html.twig' %}

{% block title %}Inscription{% endblock %}

{% block body %}
    <h1>Inscription</h1>

    {{ form_start(registrationForm) }}
        {{ form_widget(registrationForm) }}

        <button formnovalidate>Inscription</button>
    {{ form_end(registrationForm) }}
{% endblock %}
